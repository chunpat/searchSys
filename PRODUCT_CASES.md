# 产品案例、图案与数据管理

登录后打开“产品案例 / 图案”。报价结果中的“案例”按钮按该条报价精确跳转，不会把同名 SKU 的其他供应商案例混入。

管理员可以新增、编辑、关联多条报价、补充 SKU、上传原图和图案、裁剪图案、移除图片关联、配置多个定制面、启用或停用案例。查询账号只能查看已启用案例和图片，不能操作管理接口。写入使用现有 CSRF 校验，并记录操作审计；并发编辑使用版本号防止覆盖。

## 工作流程

1. 管理员导入原始供应商 `.xlsx`（不走系统管理中的标准报价数据导入）。支持 WPS `DISPIMG` 和普通 Excel 浮动图片。一个工作表行生成一个案例，同一行的多张图片归入该案例。
2. 按文件名、工作表和行匹配现有报价。结果进入“待整理”，未匹配的资料进入“未关联报价”筛选。
3. 查看原图和关联数据；添加或纠正报价关联，必要时补充 SKU。报价的共用 SKU 自动参与搜索。
4. 原图保留为“参考图”。可上传独立透明 PNG，或在原图上框选裁剪，另存为“图案素材”。裁剪不会自动去背景，也不会提升原图分辨率。
5. 选择模型，在预览上方的“模型尺寸”中填写产品尺寸（mm，1 cm = 10 mm）。杯子可改外径和高度，锥形杯可分别改杯口、杯底外径，盒子/收纳盒/袋子可改宽深高，二十面骰子使用外接球直径。杯柄不计入杯身外径，袋身高度不含提手。尚未填写的旧案例明确显示“示例尺寸”，不会从报价中的定制尺寸推断产品大小。
6. 添加定制区域。每个区域独立保存表面、图案、工艺、图案宽高、偏移、旋转角度；多个区域同时预览。未绑定图案时显示蓝色区域框。模型尺寸和图案尺寸独立，改模型不会改写图案宽高。
7. 保存，检查后设为“已启用”。停用后查询账号不能访问案例及其未被其他启用案例使用的图片。

导入只新增，不覆盖报价或已整理案例。再次导入同名工作簿时，同一工作表行已存在的案例会被跳过。如果源文件移动行或改名，应先人工检查来源关系；不会自动认定为同一案例。

线上上传还受反向代理的大小和超时限制。若导入返回 HTTP 413 或 HTML 错误页，可展开导入面板中的“服务器导入步骤”；Docker 命令和按 HTTP 状态排查的方法见 [DEPLOYMENT.md](DEPLOYMENT.md#案例-excel-上传返回-html-错误页)。请求超时不一定表示后台已经停止，先确认案例列表和服务状态，勿连续重复导入。

## 当前边界

- 3D 模型按手工填写的尺寸实时调整比例，并显示尺寸线和毫米数值；杯柄、壁厚、瓶肩等细节仍采用模板比例，不是工厂实物 CAD，也不自动从报价原文建模。修改尺寸时保留观察距离，滚轮只改变观察缩放，“适应视图 / 复位”可重新看全模型。二十面骰子的面序是示意序号，必须与工厂模板核对。
- 图案可沿圆柱或锥面显示，支持盒子多面独立图案。为避免穿出模型，预览会限制位置和缩小过大的图案，并明确提示原始宽高、实际预览宽高或位置限制；保存的宽高和偏移不会因此改变。模板的可用区域边距仅用于示意，不是供应商实际禁印范围。
- 工艺名称和尺寸规则用于关联、查阅，不会自动生成雕刻凹凸、刀模、生产展开图或合格判定。生产仍需原始文件、工厂模板及人工核对。
- “已启用”仅表示案例可供查询，不表示供应商规则、尺寸或价格已经审核通过。
- 图片格式限 PNG/JPG/WebP，单张不超过 12 MB / 2400 万像素。服务端实际解码校验、重新编码、移除元数据，并生成缩略图。SVG/HTML 不作为可执行图片接受。

## 数据保存与重建

`product_cases`、`case_images`、`case_assets` 保存在现有 SQLite 数据库中。文件保存在 `data/case_assets/`，由登录鉴权接口提供，不能通过静态目录直接访问。

案例关联采用“来源 + 供应商 + SKU + 工艺 + 材质 + 产品/定制尺寸”的稳定标识，不依赖每次重建可能变化的 `QUOTE-xxxx`。报价编号、价格改变时关联继续读取当前数据；标识字段改变或记录被删除时，显示原快照及“关联需复核”，管理员需重新选择关联。现有重建不会删除案例表或图片。

**标准数据的 Excel/JSON 导出不包含案例和图片。** 部署迁移、备份和恢复必须一起保留完整 `quote_data` 卷，或 SQLite 数据库与 `data/case_assets/`，不可只导出报价 JSON。此模块不提供单独的案例打包导出。

本地也可运行一次增量导入：

```sh
python tools/import_product_cases.py '副本供应商工艺沉淀 2026.7.16.xlsx'
```

首次升级安装 `requirements.txt` 并重启应用，启动会创建案例表，并为旧表增补 `model_dimensions` 字段；已有案例和关联保持不变，未设置的尺寸使用明确标识的示例值。Docker 镜像已复制 `app` 和 `tools`，图片目录使用现有 `/app/data` 持久卷。Three.js 0.180.0 及 MIT 许可证保存在 `app/static/vendor/three/`；运行时不访问外部 CDN。

验证：`python -m unittest discover -s tests`。案例测试使用隔离数据库，覆盖角色权限、CSRF、图片可见性、版本冲突、非法文件与裁剪、跨案例图案引用、重复导入和重建关联。

接口错误提示的回归测试：`node --test tests/test_api_response.cjs`（测试工具需要 Node.js 18+，应用运行仍不需要 Node.js）。覆盖代理 HTML 错误页、上传限制、网关超时、错误路由及正常导入响应。
